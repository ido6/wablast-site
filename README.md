# WaBlast — marketing site

Static subscription landing page. Deployed free on GitHub Pages.

- Edit prices/copy in `index.html`.
- Go live on payments: paste your Stripe Payment Link (or Gumroad/Lemon Squeezy URL) into the `CHECKOUT` object in the inline `<script>`. Until set, buy buttons capture leads by email.
